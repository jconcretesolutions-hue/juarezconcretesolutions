const menu=document.querySelector('.menu');const nav=document.querySelector('.nav nav');const closeMenu=()=>{if(!menu||!nav)return;nav.classList.remove('open');menu.setAttribute('aria-expanded','false')};if(menu&&nav){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu))}
const estimateSection=document.querySelector('#estimate');if(estimateSection){document.querySelectorAll('a[href="#estimate"]').forEach(link=>link.addEventListener('click',event=>{event.preventDefault();closeMenu();const header=document.querySelector('.header');const top=estimateSection.getBoundingClientRect().top+window.scrollY-(header?.offsetHeight||0)-16;history.pushState(null,'','#estimate');window.scrollTo({top,behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'})}))}
const dialog=document.querySelector('#lightbox');if(dialog){const image=dialog.querySelector('img');document.querySelectorAll('.photo').forEach(p=>p.addEventListener('click',()=>{image.src=p.dataset.src;dialog.showModal()}));dialog.querySelector('button').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()})}
const year=document.querySelector('#year');if(year)year.textContent=new Date().getFullYear();
